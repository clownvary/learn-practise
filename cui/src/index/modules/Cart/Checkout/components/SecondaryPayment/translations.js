import { defineMessages } from 'react-intl';

const PREFIX = 'app.modules.cart.Checkout.SeondaryPayment';

export default defineMessages({
  title: {
    id: `${PREFIX}.title`,
    defaultMessage: 'Secondary Payment Method for Future Charge'
  }
});
