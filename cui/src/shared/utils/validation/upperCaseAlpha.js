export default function upperCaseAlpha(value, msg) {
  let error = '';
  // let numericRegex = /[0-9]/;

  // if(numericRegex.test(value)){
  //     error = msg + " should not include number.";
  //     return error;
  // }

  return "";
}


export  function upperCaseAlphaFormat(value) {
  return value.toUpperCase();
}
