export default function lowerCaseAlpha(value, msg) {
  if(!value || value == "") return "";
  let error = '';
  // let numericRegex = /[0-9]/;

  // if(numericRegex.test(value)){
  //     error = msg + " should not include number.";
  //     return error;
  // }

  return "";
}


export  function lowerCaseAlphaFormat(value) {
  return value.toLowerCase();
}

