import _taggedTemplateLiteralLoose from 'babel-runtime/helpers/taggedTemplateLiteralLoose';

var _templateObject = _taggedTemplateLiteralLoose(['btn-group\n      ', '\n      ', ''], ['btn-group\n      ', '\n      ', '']);

import React, { PropTypes } from 'react';
import 'active.css/less/components/pill.less';

import { cls } from './shared/utils';

var string = PropTypes.string,
    object = PropTypes.object,
    any = PropTypes.any,
    oneOf = PropTypes.oneOf;


var propTypes = {
  className: string,
  style: object,
  size: oneOf(['xs', 'sm', 'lg', 'xl']),
  children: any
};

var Pills = function Pills(_ref) {
  var className = _ref.className,
      style = _ref.style,
      size = _ref.size,
      children = _ref.children;
  return React.createElement(
    'div',
    {
      className: cls(_templateObject, size ? 'btn-group-' + size : '', className || ''),
      style: style
    },
    children
  );
};

Pills.displayName = 'AAUIPills';
Pills.propTypes = propTypes;

export default Pills;