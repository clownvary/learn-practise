import createPopupHOC from './createPopupHOC';

// eslint-disable-next-line
const createPopup = (WrappedComponent, defaultOptions) => {
  // eslint-disable-next-line
  const hoc = createPopupHOC(WrappedComponent);
  const popup = {};
  return popup;
};

export default createPopup;
