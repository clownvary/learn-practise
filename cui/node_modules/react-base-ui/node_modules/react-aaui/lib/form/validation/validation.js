'use strict';

exports.__esModule = true;
exports.isEmpty = isEmpty;
/**
 * return false if val is empty
 *
 * @param val
 * @returns {boolean}
 */
function isEmpty(val) {
  return val == null || typeof val === 'string' && !val.trim();
}