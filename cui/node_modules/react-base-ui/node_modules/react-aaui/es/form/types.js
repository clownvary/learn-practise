import { PropTypes } from 'react';

var string = PropTypes.string,
    object = PropTypes.object,
    func = PropTypes.func,
    shape = PropTypes.shape,
    any = PropTypes.any;


export var FormPropTypes = {
  onSubmit: func,
  onFail: func,
  onChange: func,
  defaultValues: object
};

export var FormFieldAPIPropTypes = {
  api: shape({
    setValue: func,
    setError: func
  })
};

export var FormFieledPropTypes = {
  rules: string,
  value: any,
  parser: func,
  formatter: func,
  validator: func,
  errMsg: string
};